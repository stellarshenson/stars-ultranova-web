# Defects - Stars Ultranova Web

`[ ]` open, `[x]` fixed. Dated notes under each track how it evolved. Sources: functional browser harness (tests/functional/), LLM playtest forensics, wave verifiers.

## API

- [x] `DEF-1` **waypoint task name asymmetry** - commands accept task 'LayMines' but player-state returns task_type 'LayMinesTaskObj' (Python class name leaks into the API); UI compensates in taskDisplayName but API consumers must handle both spellings; fix: normalize task_type serialization to the command vocabulary; `backend player-state serialization`
  - 2026-07-27 reported: reported by functional harness (test_fleet_waypoints)
  - 2026-07-28 fixed: task_type now serialized via get_task_name (TASK_COMMAND_NAMES in backend/core/waypoints/waypoint.py) in both game_manager waypoint payloads, so player-state and the /fleets/{key}/waypoints endpoint emit the same names WaypointTaskBase.from_dict accepts (NoTask/Cargo/Colonise/LayMines/Invade/Scrap/SplitMerge/RemoteMine); the now-dead .replace('TaskObj','') compensation removed from fleet-panel.js taskDisplayName (cache-buster v8 -> v9) and from llm_playtest fmt_waypoints; regression TestWaypointTaskVocabulary (3 tests) in tests/unit/test_api.py
- [x] `DEF-5` **components endpoint hides race restrictions** - GET /api/designs/components omits race availability (Component.is_available_to_race is server-only), so clients cannot pre-filter trait-restricted components - designs with Settler's Delight / Fuel Mizer engines rejected only at submit with 'not available to your race'; fix: expose availability or restriction traits in the payload; `backend/api/routes`
  - 2026-07-28 reported: reported by LLM playtest harness smoke (order_rejected forensics)
  - 2026-07-28 fixed: ComponentResponse now carries required_traits and forbidden_traits (RaceRestriction REQUIRED / NOT_AVAILABLE trait codes) on both GET /api/designs/components and /components/{name}, so a client can pre-filter before submitting a design - Settler's Delight required_traits ['HE'], Fuel Mizer ['IFE']; regression TestDesignCatalogAvailability in tests/unit/test_api.py
- [x] `DEF-6` **hulls endpoint omits tech requirements** - LOW; GET /api/designs/hulls lacks tech_requirements so buildability must be inferred by joining against the components list by name; fix: include tech requirements on hull payloads; `backend/api/routes`
  - 2026-07-28 reported: reported by LLM playtest harness smoke
  - 2026-07-28 fixed: HullResponse now carries tech_requirements on GET /api/designs/hulls and /hulls/{name}, taken from the same Component.required_tech.levels the components list exposes, so hull buildability no longer needs a name join; regression TestDesignCatalogAvailability asserts hull and component tech match for all 38 hulls
- [x] `DEF-17` **empire password gate missing on all fleet mutations** - HIGH; _require_password guards 5 games.py handlers but all 7 mutating fleets.py endpoints (rename, battle-plan, split, merge, cargo, fleet transfer, gift) and games.py get_battles took empire_id from the request body unchecked, so in correspondence play any participant could command another empire's fleets and read its battle intel; fix: import _require_password and gate all 8 handlers with an X-Empire-Password header; `backend/api/routes/fleets.py` `backend/api/routes/games.py`
  - 2026-07-28 reported: reported by adversarial review (architect, CRITICAL)
  - 2026-07-28 fixed: X-Empire-Password header + _require_password call added to all 7 fleets.py mutations and games.py get_battles; unit+e2e 946 passed
- [x] `DEF-21` **CORS middleware for an origin that never exists** - LOW; backend/main.py added CORSMiddleware with four settings keys (cors_origins/cors_allow_credentials/cors_allow_methods/cors_allow_headers in backend/config.py and project.env) although the app always serves its own frontend same-origin - index.html derives its base href client-side and client.js fetches the relative 'api' path - and every non-browser consumer (tests, harnesses, scripts) uses urllib, so no request ever carried a cross-origin Origin header; the config invited operators to tune a knob with no effect; fix: drop the middleware, its import, the config keys and the project.env block; `backend/main.py` `backend/config.py` `project.env`
  - 2026-07-28 reported: reported by adversarial review (architect)
  - 2026-07-28 fixed: CORSMiddleware block and its import removed from backend/main.py, the four cors_* keys removed from backend/config.py and project.env, stale 'CORS origins' bullet dropped from the README configuration list; app boots with 45 routes, suite green
- [x] `DEF-26` **waypoint enum task silently degrades to NoTask across a round trip** - LOW; Waypoint.to_dict emitted the raw enum name for a bare-enum task (`{"type": self.task.name}` -> "LAY_MINES"), but WaypointTaskBase.from_dict normalizes that to "lay_mines", which matches no branch and falls through to NoTaskObj - so a mine-laying, colonise or invade order held as an enum vanished across any save/load; latent today only because the sole enum assignment in the tree is NO_TASK, which round-trips correctly by accident; cause: two vocabularies for one field, with TASK_COMMAND_NAMES already present and unused on this path; fix: serialize the enum branch through TASK_COMMAND_NAMES; `backend/core/waypoints/waypoint.py`
  - 2026-07-28 reported: found during DEF-1 triage by the API agent, reported as a latent round-trip bug and left unactioned
  - 2026-07-28 fixed: enum branch now serializes via TASK_COMMAND_NAMES; new tests/unit/test_waypoint_roundtrip.py verified failing first (5 of 9 failed: SPLIT_MERGE/REMOTE_MINE/LAY_MINES etc all restored as NO_TASK, and 'LAY_MINES' != 'LayMines'), all 9 pass after

## Client UI

- [x] `DEF-2` **race wizard re-render loses in-flight input** - MEDIUM; every field change triggers full wizard re-render (updateField -> render), so typing Race Name then Plural Name loses the plural - first field's blur re-render resets the second input before its change commits; debounced /races/validate also briefly disables Save Race and can swallow the click; fix: re-render only derived sections or preserve focus/values; `frontend/js/views/race-wizard.js`
  - 2026-07-27 reported: reported by functional harness (test_race_designer, reproducible)
  - 2026-07-28 fixed: RaceWizard.updateField no longer re-renders the wizard (frontend/js/views/race-wizard.js) - the control that fired the change already shows its value, so only derived state updates: syncImmunityInputs toggles the min/max inputs of an immune axis (env groups tagged data-axis) and updatePointsDisplay refreshes the footer; the Save button is no longer disabled while /races/validate is pending (only for a known-negative total) and saveRace awaits new ensurePointsSettled, which flushes the debounce timer and the round-trip so a click inside the debounce window still saves; index.html race-wizard.js v7 -> v8; functional regressions test_race_designer_keeps_typed_input_across_fields (typed plural was reset to 'Humanoids' before the fix) and test_race_designer_save_click_during_validate (Save disabled while pending before the fix)
- [x] `DEF-3` **empty turn report dialog every turn** - turn report dialog opens even with zero messages ('No messages this turn') forcing a dismiss each turn; cause: app.js checks only GameState.game where the comment says show-if-messages; fix: gate on message count; `frontend/js/app.js`
  - 2026-07-27 reported: reported by functional harness (test_turn_and_messages)
  - 2026-07-28 fixed: turn report gated on message count in frontend/js/app.js turnGenerated (GameState.messages.length > 0), matching the comment that was already there; index.html app.js v8 -> v9; tests/functional/helpers.py generate_turn now dismisses the report only when the turn carried messages and waits for the year indicator, since GameState.game.turn flips one async hop before the app finishes handling the turn; functional regression test_turn_report_only_opens_with_messages - before the fix every zero-message turn (2101 onward, seed 7777) opened 'No messages this turn'
- [x] `DEF-19` **panel-manager.js dead and duplicating panel show/hide** - MEDIUM; frontend/js/views/panel-manager.js (397 lines) was loaded on every page and PanelManager.init() called from app.js, yet 9 of its 23 methods had zero callers and addHeaderControls - the only creator of .pin-btn/.collapse-btn markup - was never invoked, making pin and collapse unreachable; its live half re-implemented show/hide on starSelected/fleetSelected/selectionCleared, which star-panel.js and fleet-panel.js already do themselves on the same events, so two owners toggled the same panels; fix: delete the file, its script tag, the init call and the orphaned pin/collapse CSS; `frontend/js/views/panel-manager.js` `frontend/css/main.css`
  - 2026-07-28 reported: reported by adversarial review (architect)
  - 2026-07-28 fixed: frontend/js/views/panel-manager.js deleted (397 lines) after confirming star-panel.js and fleet-panel.js each subscribe to starSelected/fleetSelected/selectionCleared and toggle their own .hidden class; script tag and PanelManager.init() removed, orphaned .panel-controls/.pin-btn/.collapse-btn/.panel.collapsed rules dropped from main.css; app.js cache-buster v=6 -> v=7
- [x] `DEF-20` **stars-theme.css a phantom palette overriding one live rule** - LOW; frontend/css/stars-theme.css declared a 20-token :root Stars! palette that main.css never referenced (zero var() in main.css) and 6 of its 7 classes (.star-uninhabited/.star-friendly/.star-enemy/.star-selected/.resource-bar-fill/.hab-indicator) were never emitted by any view; the seventh, .resource-bar, silently overrode main.css's own .resource-bar/.concentration-bar rule purely because the stylesheet loaded second - the shipped 12px/#202050/2px look came from a file everyone read as dead; fix: fold the three winning properties into main.css then delete the stylesheet and its link; `frontend/css/stars-theme.css` `frontend/css/main.css`
  - 2026-07-28 reported: reported by adversarial review (architect)
  - 2026-07-28 fixed: the three winning .resource-bar properties (height 12px, background #202050, radius 2px) folded into main.css as an override after the shared .resource-bar/.concentration-bar rule so rendering is unchanged, then frontend/css/stars-theme.css deleted (98 lines) and its link removed; main.css cache-buster v=10 -> v=11 (test_panel_polish assertion updated to match)
- [x] `DEF-23` **battle replay renders empire 2 off-canvas and dates every battle year 0** - MEDIUM; BattleReport.year is never assigned by either engine so replays read 'Battle at Sol - 0'; and the viewer computes cellSize = 400/grid_size (100) while stack positions live on the 0-1000 GRID_SIZE scale, so the second empire's stacks map beyond the 400px canvas; fix: assign year at report creation and divide positions by GRID_SCALE in the viewer; `frontend/js/views/battle-viewer.js` `backend/server/battle/battle_report.py`
  - 2026-07-28 reported: reported by battle-doctrine design exploration
  - 2026-07-28 fixed: both engines now stamp battle.year = server_state.turn_year at report creation, and the viewer reads grid_size as GRID_SCALE units-per-square (gridScale), fixes the board at 10 squares and divides positions by gridScale in cellCenter so every stack lands inside the 400px canvas; regressions TestBattleReportYear and tests/unit/test_battle_viewer.py
- [x] `DEF-28` **panel-polish test pins a stale main.css cache-buster** - LOW; tests/e2e/test_panel_polish.py:53 asserts 'static/css/main.css?v=12' is served, but frontend/index.html now links main.css?v=13 after a later styling pass, so the test fails on a version string rather than on any rendered behaviour - it was red at the battle-doctrine baseline and is unrelated to combat; fix: assert the link exists with any version, or update the pinned number when the stylesheet is bumped; tests/e2e/test_panel_polish.py
  - 2026-07-28 reported: LOW; tests/e2e/test_panel_polish.py:53 asserts 'static/css/main.css?v=12' is served, but frontend/index.html now links main.css?v=13 after a later styling pass, so the test fails on a version string rather than on any rendered behaviour - it was red at the battle-doctrine baseline and is unrelated to combat; fix: assert the link exists with any version, or update the pinned number when the stylesheet is bumped; tests/e2e/test_panel_polish.py
  - 2026-07-28 fixed: fixed - the pin now asserts main.css?v=13, the version index.html has served since the .imminent-battle warning block landed in 1a8a6f5; tests/e2e/test_panel_polish.py

## Gameplay

- [x] `DEF-4` **first-turn message drought** - LOW; first turns on seeds 1111/4242/5555/6666 produce no player messages at all so the message panel stays hidden after turn 1; first star-linked message appears only when production completes; assess whether canonical Stars! emits early-game messages (research, growth) and add if so; `backend/server/turn_generator.py`
  - 2026-07-27 reported: reported by functional harness (multiple seeds)
  - 2026-07-28 fixed: canon check first: original Stars! Nova DOES emit a first-turn message - StarMapInitialiser.cs:113-117 GeneratePlayerAssets appends 'Your race is ready to explore the universe.' with Audience = Global.Everyone (GlobalDefinitions.cs:140-141, Everyone == Nobody == 0); our create_game cleared all_messages after the initial scan and never seeded it, so turn 1 was silent. Ported the welcome message into GameManager.create_game (all_messages + _last_messages) so it reaches every empire's player-state and MessagePanel.loadMessages shows the panel on gameCreated/gameLoaded. No flavour messages invented: the other canonical early-game emitters (tech advance StarUpdateStep.cs:192, new component :207-234, colonists killed :73, production complete Manufacture.cs:104) are already ported and correctly emit nothing on a turn where nothing happens. Regression TestFirstTurnMessages in tests/unit/test_api.py
- [x] `DEF-7` **fuel burn linear in warp instead of canonical per-engine tables** - HIGH; playtest commander empirically measured burn approx mass x ly x 0.005 x warp (0.025 at w5, 0.030 at w6, 0.040 at w8) - a smooth linear function, where original Stars! uses per-engine per-warp fuel tables (references/original-game/Common/Components/Engine.cs) and the acc-crit Fuel fidelity criterion claims (mass+cargo) x engine table x warp^2/200; either the movement code bypasses the tables or components.xml engines carry simplified data; fix: port canonical per-engine burn tables with our documented mods; `backend/core` `backend/data/components.xml`
  - 2026-07-28 reported: reported by run100 playtest (Silicon Loom, turns 3-4); user directive: 'we need stars! Original burn tables with some our mods'
  - 2026-07-28 fixed: fixed - canonical per-engine per-warp tables now drive both live movement and estimates: ShipToken caches the engine fuel table (Engine.cs:34), starting SimpleDesigns mount Quick Jump 5 / Settler's Delight (StarMapInitialiser.cs:140-151), Fleet.fuel_consumption ports ShipDesign.cs:721-744 ((mass+cargo) x table[warp-1]/100 x warp^2/200, IFE x0.85) with Fleet.cs:586-608 cargo distribution, TurnGenerator._consume_fuel delegates to it; free-warp detection accepts the web negative ramscoop entries (<= 0); proven by tests/unit/test_fuel_tables.py (26 tests) and seeded e2e tests/e2e/test_fuel_burn.py - measured scout burn 3/8/64 mg at w5/w6/w8 matches table entries 100/180/800 exactly and refutes the linear model's 15/27; suite 989 green
  - 2026-07-28 boundary note: the detached run100 playtest server on port 9830 still holds the OLD linear fuel physics in memory; if its harness restarts that server the canonical tables take effect mid-run - playtest fuel data spanning a restart is not comparable
  - 2026-07-28 run100 forensics confirm consequence: old linear physics plus free_warp 0 starting designs stranded 14 empire-1 fleets at warp 0 (16 stuck_fleet events); stranding root cause fixed in the DEF-7 change set, residual placeholder-warp and save-migration gaps tracked as a new in-transit waypoint defect
- [x] `DEF-8` **score formula makes escort spam strictly dominant** - HIGH; escort ships score +2 per SHIP with no cost or mass scaling (deliberate documented deviation from ServerState/Scores.cs per-token, siding with legacy Common/Scores.cs and canonical Stars!), tech capped at a flat +4 bracket and planets adding ~0 directly - run100 empire 2 scored 2657 (2536 of it from 1268 cheap armed probes, 4 planets, 0% research) vs the victor's 264 with 54 planets, so probe spam and zero research are score-optimal; score gates three victory targets (victory_check.py:66-69, 209-217) making any score-victory game trivially exploitable; fix needs a design call - pick the canon (per-token vs per-ship), then cap or cost-scale the ship score terms; `backend/server/scores.py` `backend/server/victory_check.py`
  - 2026-07-28 reported: reported by run100 playtest forensics
  - 2026-07-28 fixed: kept the exact ServerState/Scores.cs formula but capped unarmed/escort ship POINTS at one scoring ship per owned planet (documented web mod in scores.py citing canonical original-Stars! 'The Score' semantics; the capital harmonic Scores.cs:135-139 already saturates at 8*planets; Nova C# omitted the cap in both Scores.cs variants which is exactly why escort spam was unbounded) - raw unarmed/escort/capital counts in ScoreRecord stay uncapped for the report UI; run100 shape now scores expansion above spam (seeded e2e tests/e2e/test_score_victory.py: 300-probe 4-planet empire strictly below a 40-planet empire), unit cap/boundary tests in tests/unit/test_scores.py; functional golden gamethrough_seed4242.json honestly regenerated 38 -> 34 via RUN_FUNCTIONAL=1 harness launcher (planets=1 caps the scout fleet's unarmed points; fleets/planets/year unchanged); victory gates consume ScoreRecord.score unchanged
- [x] `DEF-9` **negative-hab deaths floor-rounded to a flat 100/year** - MEDIUM; star.py:419-420 rounds population growth with Python floor division (int(g) // 100) * 100 which floors toward negative infinity, where C# Star.cs:351-355 truncates toward zero - computed deaths of 1-99 colonists become -100 (canon: 0) and larger deaths over-round by up to 99, yielding the observed flat '100 killed' per year on negative-hab worlds independent of population and hab severity; fix: truncate toward zero for negative growth; `backend/core/game_objects/star.py`
  - 2026-07-28 reported: reported by run100 playtest forensics
  - 2026-07-28 fixed: star.py calculate_growth now truncates toward zero for negative growth (C# (int) cast + integer division, Star.cs:380-383) - deaths of 1-99 colonists round to 0 and larger deaths no longer over-round by up to 99; proven by tests/unit/test_star.py negative-hab growth tests and seeded e2e tests/e2e/test_hostile_world_growth.py (exact year-by-year colonist series, proportional decay); regression test_parity.py over-capacity expectation updated to the truncation canon
- [x] `DEF-10` **mineral-blocked production order starves forever** - HIGH; _manufacture_items breaks with zero spend and zero mineral banking when on-hand minerals are below one unit's cost (only energy progress is banked), then the outer loop lets trailing orders consume the exact minerals the head order awaits - permanent starvation, reproduced at Zubenelgenubi and exploited as a self-regulating queue at T53; C# canon (FactoryProductionUnit.cs:108-142, ShipProductionUnit.cs:137-178) does a proportional partial build spending the scarce mineral to exactly 0 and banks per-resource RemainingCost, which also trips trailing IsSkipped exhaustion checks so non-auto orders block the queue; fix: port partial builds with per-resource banking and queue blocking; `backend/server/turn_steps/star_update_step.py:504-508`
  - 2026-07-28 reported: reported by run100 playtest forensics
  - 2026-07-28 fixed: ported C# per-resource RemainingCost banking - ProductionOrder gains remaining_cost (serialized, legacy energy-scalar saves migrate on first manufacture pass; partial_resources_spent kept as derived energy mirror for client percent display and AI heuristics), _manufacture_items now does the Construct proportional partial build (FactoryProductionUnit.cs:108-142, ShipProductionUnit.cs:137-180) spending the scarce commodity to exactly 0 with ceil-per-commodity rounding (Resources.cs:251-261, faithful overshoot), which trips trailing non-auto IsSkipped so the queue blocks per Manufacture.cs:56-61 instead of starving the head; documented C# defect fixed in the port: remaining cost resets to full unit cost after each completed unit (FactoryProductionUnit.cs:136-141 never resets, NeededResources ProductionOrder.cs:79-93 shows the intended invariant); ADD anti-tamper extended to reject remaining_cost; proven by tests/unit/test_production_banking.py (13 tests incl. Zubenelgenubi starvation regression) and seeded e2e tests/e2e/test_production_banking_e2e.py
- [x] `DEF-11` **in-transit placeholder waypoint serializes phantom warp 6 and eats warp edits** - MEDIUM; turn_generator.py:410-416 inserts the in-transit 'Space at X,Y' placeholder waypoint without warp_factor so it takes the Waypoint dataclass default 6, where C# TurnGenerator.cs:430-436 copies waypointZero.WarpFactor - game_manager.py reports waypoints[0].warp_factor as fleet warp so every in-transit or stranded fleet shows warp 6 regardless of speed, and fleet.warp_factor/speed setters write to this placeholder which the same-position pop discards next turn, silently losing warp edits made in transit; also pre-DEF-7-fix saves deserialize token free_warp_speed=0 (fleet.py:194) and the web-only 'dropped to warp N' message duplicates the canonical out-of-fuel message; fix: copy the real warp onto the placeholder, route setters to the destination waypoint, migrate free_warp on load; `backend/server/turn_generator.py` `backend/core/game_objects/fleet.py`
  - 2026-07-28 reported: reported by run100 playtest forensics
  - 2026-07-28 fixed: the in-transit placeholder now copies waypointZero's warp (TurnGenerator.cs:430-436) so in-transit fleets serialize their true - possibly free-warp-clamped - speed; warp edits made in transit write through the placeholder onto the destination waypoint (documented web deviation - the C# WinForms client edits leg waypoints directly), both via Fleet.speed setter and WaypointCommand Edit at index 0; the web-only 'dropped to warp N' duplicate message removed (Fleet.cs:570-576 clamps silently, the canonical per-turn message remains); pre-DEF-7 saves migrate on load - game_manager._deserialize_state refreshes token free_warp_speed/fuel_table from the empire design, deriving from a stored non-zero table when the design is gone; proven by tests/unit/test_turn_generator.py (TestInTransitWarp, TestFreeWarpSaveMigration) and seeded e2e tests/e2e/test_transit_waypoints.py; suite 1033 green
- [x] `DEF-12` **colonize task auto-invades occupied planets** - HIGH; post_bombing_step.py:96-102 converts a COLONIZE task on a foreign-owned planet into a full unordered invasion (run100 Kapteyn's Star: 2200x1.1 attackers vs 500 defenders, 455 killed, colonizer hull survives because the invasion path never consumes tokens), where C# ColoniseTask.cs:88-92 IsValid aborts with 'already occupied' and keeps colonists aboard; _perform_colonization also lacks an occupied-planet guard (own-planet colonize overwrites star.colonists) and consumes only can_colonize tokens instead of C# whole-fleet Composition.Clear() with 75% cost salvage; fix: port the IsValid guard and canonical fleet consumption; `backend/server/turn_steps/post_bombing_step.py`
  - 2026-07-28 reported: reported by run100 playtest forensics
  - 2026-07-28 fixed: colonize never converts to invasion - _perform_colonization now runs the ColoniseTask.cs IsValid guards in C# order (occupied ColoniseTask.cs:88-92 covering foreign AND own planets, colonists aboard 94-98, colonization module 100-104) with the canonical 'attempted to colonise ... but it is already occupied.' texts, aborting with colonists kept aboard and fleet intact; explicit INVADE orders untouched; kept the adjudicated-better-than-canon colonizer-token-only consumption with the deviation comment strengthened to cite ColoniseTask.cs:119-127 (C# clears the whole fleet + 0.75*cost salvage) - flagged as a design call for the orchestrator; proven by tests/unit/test_turn_generator.py TestPostBombingStep occupied-guard tests and seeded e2e tests/e2e/test_colonize_guard.py; suite 1033 green
- [x] `DEF-13` **zero-fuel fleets move full distance then deadlock silently** - HIGH; _move_fleet moves the full ordered distance BEFORE fuel deduction where C# Fleet.cs Move caps travel by fuelTime so empty tanks do not move; _consume_fuel then clamps warp to free_warp_speed (0 for standard engines pre-DEF-7-fix; the ported-but-unused Fleet.move floors at max(1, free_warp)) and _move_fleet:489-490 returns 'in_transit' at warp<=0 with no message - run100 Santa Maria #64 burned to 0 fuel, moved 4.0 ly on an empty tank and stranded a partial-move residue 0.0416 ly short of LHS 292 (displayed rounded as ON the planet), so the colonize waypoint never reached waypoint-zero state, PostBombingStep never fired and the commander got zero feedback for the rest of the run; fix: port C# fuel-time distance capping and emit a stranded message; `backend/server/turn_generator.py` `backend/core/game_objects/fleet.py`
  - 2026-07-28 reported: reported by run100 playtest forensics
  - 2026-07-28 fixed: ported the Fleet.cs Move three-way travel-time min (target/available/fuel time, lines 520-546 incl. the >= fuel comparison at 542) into turn_generator._move_fleet, folding _consume_fuel in so live burn and cap are one formula - an empty tank now moves a fleet 0 ly and fuel never goes negative (fuel used int-truncated per Fleet.cs:567); out-of-fuel drop to free warp is silent per Fleet.cs:570-576; web extensions documented in the docstring: dust nebulae scale speed AND burn rate together (burn per ly at the ordered warp unchanged) and a fleet left in transit at effective warp 0 gets a per-turn 'stranded in deep space' message instead of the canonical silent deadlock; proven by tests/unit/test_turn_generator.py TestMoveFleetFuelCap (three-way min, >= edge, zero-move, stranded) and seeded e2e test_fuel_burn.py test_fuel_time_caps_travel_distance (Teamster covers 54.17 not 64 ly, then crawls at free warp 1); suite 1033 green
- [x] `DEF-14` **battle engine counts unarmed flee-targets, spawning phantom battles forever** - HIGH; ron_battle_engine.py _select_targets (306-365, the active engine per server_data.py use_ron_battle_engine=True) appends enemies to an UNARMED wolf's target list for flee movement and counts that wolf in number_of_targets, where C# BattleEngine.cs:412-415 skips unarmed wolves entirely so unarmed-vs-unarmed co-location yields no battle - two stranded unarmed ships (e1 Teamster #34 Small Freighter vs e2 Long Range Scout #9) each registered as targeted and a 60-round movement-only battle with zero possible attacks ran and reported EVERY turn, producing run100's verbatim 12-year Sabik standoff; fix: exclude unarmed wolves from target selection and battle triggering per canon; `backend/server/battle/ron_battle_engine.py`
  - 2026-07-28 reported: reported by run100 playtest forensics
  - 2026-07-28 fixed: only ARMED wolves count as battle triggers in ron_battle_engine._select_targets (BattleEngine.cs:412-415) - unarmed wolves still get flee target lists for movement, but unarmed-vs-unarmed co-location now returns 0 targets so run() aborts before location/stacks/report (the C# double gate) and _do_battle ends once only unarmed survivors remain; proven by tests/unit/test_battle_engine.py TestUnarmedBattleTrigger and seeded e2e tests/e2e/test_phantom_battles.py (run100 Sabik reproduction: 3 turns, zero battles; armed-vs-unarmed still fights); suite 1082 green
- [x] `DEF-15` **battle messages drop the loss summary** - MEDIUM; _execute_battles (turn_generator.py:1710-1746) emits only 'A battle took place at {location}', dropping the C# loss summary (BattleEngine.cs:945-953 appends 'N of your ships were destroyed') and the attached report event - battle_engine.py:771-792 even builds that text then discards it; the full replay IS distributed to empire.battle_reports and served by GET /empires/{id}/battles, but message-only consumers (and run100 commanders) see losses only by fleet disappearance (Wolf 359 colonizer death); fix: append the per-empire loss summary to the battle message and attach the report reference; `backend/server/turn_generator.py` `backend/server/battle/battle_engine.py`
  - 2026-07-28 reported: reported by run100 playtest forensics
  - 2026-07-28 fixed: _execute_battles battle messages now carry the ReportBattle loss summary (BattleEngine.cs:945-953) - 'None of your ships were destroyed.' / 'N of your ships were destroyed.' appended per empire from battle.losses (initialized 0 for every participant at _position_stacks); star_name keeps carrying the location for the client goto so no Message schema change (report-reference sub-item dropped as the zero-schema-change option the plan offered); the standard engine's dead _report_battle text-building removed and its per-empire duplicate battle append collapsed to one entry per battle as the Ron engine; proven by tests/unit/test_turn_generator.py TestBattleLossSummary and e2e test_phantom_battles.py loss-summary assertions
- [x] `DEF-16` **homeworld placement has no fairness constraint - player 2 gets sparse corner starts** - HIGH; _select_home_worlds (galaxy_generator.py:761-839) picks homeworlds from the already-generated center-heavy GMM star field, giving player 1 a density-weighted pick near the centroid and each later player the greedy farthest-point star - by construction the sparsest periphery - with no check on local star density or neighborhood parity, where C# StarMapGenerator.cs (93-107, 154-170, 211-236, 288-295) places homeworlds FIRST by rejection sampling over a UNIFORM density field with enforced minimum separation (100 ly for 2 players on a 400 map) then generates stars uniformly so every homeworld gets a statistically equivalent neighborhood; run100: e1 Gliese 1 had 9/19/57 stars within 50/80/150 ly vs e2 Zubeneschamali 1/2/12 with second-nearest 80 ly, driving the 54-vs-4 planet outcome independent of the fuel model; fix: port homeworlds-first uniform placement or add a neighborhood-parity constraint; `backend/services/galaxy_generator.py`
  - 2026-07-28 reported: reported by run100 playtest forensics
  - 2026-07-28 fixed: _select_home_worlds now selects under a fairness constraint (documented web deviation - C# StarMapGenerator.cs:93-106,156-172,215-238 places homeworlds first on a uniform field; the web keeps the GMM aesthetic and delivers the guarantee by selection): every homeworld needs >= N_min = max(3, ceil(0.5*median)) neighbor stars within 50 ly (2*STAR_DENSITY), mutual separation >= min(width,height)/(2*(floor(sqrt(players))+1)) per StarMapGenerator.cs:160-163 with a 0.9-stepwise relaxation ladder (floor half) then density relaxation then plain farthest-point fallback for tiny maps; star field rng untouched (layouts per seed unchanged), player 1 stays a single rng.choice draw; seed 4242 now 10-vs-4 stars within 50 ly (was 9-vs-1 with second-nearest 80 ly); proven by tests/unit/test_galaxy_fairness.py (10 seeds x 2/4 players: floor, separation, determinism); seeded fallout honestly updated - test_terraforming SEED 20260713->20260716 (old nearest star turned hostile 92/52/76), test_packets driver colony now nearest star >= 26 ly (was 18 ly, packet arrived year 1); functional golden regenerated via RUN_FUNCTIONAL=1 twice - identical digest (fleets 7, planets 1, score 34, year 2130); suite 1082 green
- [x] `DEF-22` **repair tenders never repair - heals_others_percent never reaches a token** - MEDIUM; Hull HealsOthersPercent (Fuel Transport 5, Super-Fuel Transport 10) is parsed into Hull.heals_others_percent, serialized on ShipToken and consumed by turn_generator repair, but ship_specs.make_token never copies it and neither ShipDesign nor SimpleDesign expose it - so the field is 0 for every ship ever built and the repair-tender role has no working signal; fix: add the property to ShipDesign/SimpleDesign and copy it in make_token; `backend/services/ship_specs.py`
  - 2026-07-28 reported: reported by battle-doctrine design exploration
  - 2026-07-28 fixed: heals_others_percent now survives the design-to-token hop - added the ShipDesign property reading Hull.heals_others_percent, the SimpleDesign field with to_dict/from_dict, and the copy in ship_specs.make_token; regression TestRepairTenderChain builds a real Super-Fuel Transport from components.xml and shows the fleet repairs 12 percent instead of 2
- [x] `DEF-24` **beam range dissipation never applied - standing off is strictly better** - HIGH; canonical Stars! beams lose 10 percent damage per square of range, which is the entire reason a fleet closes; _fire_beam applies no distance falloff and the helpers WeaponDetails.beam_dispersal / beam_dispersal_ron exist but have zero callers, so beams hit as hard at maximum range as at point blank and every closing tactic is strictly worse than standing off; must be fixed BEFORE any stance layer or the stance is degenerate on day one; fix: wire the existing dispersal helpers into _fire_beam; `backend/server/battle/ron_battle_engine.py` `backend/server/battle/weapon_details.py`
  - 2026-07-28 reported: reported by battle-doctrine research (docs/research-battle-doctrine.md); canon recovered from BATTLE.TXT and Guts of the Battle Engine via the Wayback Machine
  - 2026-07-28 fixed: beam range dissipation wired into the Ron beam path - _execute_attack now multiplies hit_power by WeaponDetails.beam_dispersal_ron(distance_squared, GRID_SCALE_SQUARED)/100 before _fire_beam, so a beam keeps 100 percent in the target's square and 90 percent at its maximum range (BATTLE.TXT); regression TestBeamRangeDissipation checks the curve at every range step and both seeded e2e beam expectations were updated for the new factor
- [x] `DEF-25` **battle initiative order inverted - lowest fires first** - MEDIUM; _generate_attacks sorts all_attacks against an ascending WeaponDetails.__lt__ on initiative, so the LOWEST-initiative weapons fire first; canon fires highest initiative first, which inverts the value of battle computers and initiative-boosting hulls throughout the game; fix: reverse the sort; `backend/server/battle/ron_battle_engine.py`
  - 2026-07-28 reported: reported by battle-doctrine research
  - 2026-07-28 fixed: _generate_attacks now sorts all_attacks with reverse=True so the highest-initiative weapon fires first (BATTLE.TXT round order); regression TestInitiativeOrder in tests/unit/test_battle_engine.py asserts [200, 10]

## Architecture

- [x] `DEF-18` **dead websocket subsystem shipped in the app** - MEDIUM; backend/api/websocket.py (263 lines: ConnectionManager, handle_websocket, EventTypes, notify_turn_generated/notify_command_submitted/broadcast_to_empire) was wired into main.py at /ws/games/{game_id} and re-exported from backend/api/__init__.py, but no frontend code ever opened a socket (zero WebSocket/ws:// in frontend/js) and none of the notify_* helpers had a caller - a live public endpoint with no client and no test; fix: delete the module, the route block, the WebSocket/Optional imports and the api package exports; `backend/api/websocket.py` `backend/main.py`
  - 2026-07-28 reported: reported by adversarial review (architect)
  - 2026-07-28 fixed: backend/api/websocket.py deleted (263 lines), /ws/games/{game_id} route and handle_websocket/WebSocket/Optional imports stripped from backend/main.py, backend/api/__init__.py reduced to its docstring; no test imported the module; suite 1082 passed / 8 skipped

## Combat

- [x] `DEF-27` **battle damage on survivors discarded - the engine writes back destruction alone** - HIGH; RonBattleEngine builds StackToken copies (stack.py from_ship_token) and _destroy_stack is the only path that touches the fleet, so armour lost by a ship that SURVIVED a battle vanished when the stacks were dropped - a fleet shot to one armour point sailed on whole. C# Nova hands the fleet's own ShipToken to the stack by reference (BattleEngine.cs:264 'new Stack(fleet, stackId, token)') and ShipToken.Damage (ShipToken.cs:70-75) reads the loss straight back as a percentage, so damage is permanent in canon until repaired. Consequence beyond parity: kills were the only currency the game booked, so every doctrine axis that buys survivability was worthless and any order buying damage output dominated - the recorded cause of the stance and posture axes failing their anti-degeneracy bar; fix: write the surviving stack's armour fraction back onto the fleet token's damage_percent after the rounds; backend/server/battle/ron_battle_engine.py
  - 2026-07-28 reported: HIGH; RonBattleEngine builds StackToken copies (stack.py from_ship_token) and _destroy_stack is the only path that touches the fleet, so armour lost by a ship that SURVIVED a battle vanished when the stacks were dropped - a fleet shot to one armour point sailed on whole. C# Nova hands the fleet's own ShipToken to the stack by reference (BattleEngine.cs:264 'new Stack(fleet, stackId, token)') and ShipToken.Damage (ShipToken.cs:70-75) reads the loss straight back as a percentage, so damage is permanent in canon until repaired. Consequence beyond parity: kills were the only currency the game booked, so every doctrine axis that buys survivability was worthless and any order buying damage output dominated - the recorded cause of the stance and posture axes failing their anti-degeneracy bar; fix: write the surviving stack's armour fraction back onto the fleet token's damage_percent after the rounds; backend/server/battle/ron_battle_engine.py
  - 2026-07-28 fixed: fixed: RonBattleEngine._do_battle ends with _write_back_damage, which carries each surviving stack's lost armour fraction onto the fleet token's damage_percent (compounded with damage the token already carried, capped at 99 percent, destroyed stacks and starbase-only stacks skipped); the repair step heals it exactly as it heals withdrawal damage, and _apply_withdrawal_consequences still adds its flat WITHDRAWAL_DAMAGE_PERCENT on top. This is the canonical rule, not a web mod - C# passes the fleet's own ShipToken into the stack. Anti-degeneracy consequence measured: with damage discarded, Aggressive stance and Standard posture won every matchup; with it written back the stance and posture matrices are contested in both directions at 3 and 9 seeds; proven by tests/unit/test_battle_doctrine.py TestBattleDamageCarriesOut (5 tests) and tests/unit/test_battle_degeneracy.py
- [ ] `DEF-29` **battle balance bars red before the boarding wave** - MEDIUM; tests/unit/test_battle_balance.py (untracked, added by an earlier agent in this wave) is 4 red at the boarding baseline and was red before any boarding change: test_no_weapon_class_dominates_at_equal_cost (early tier standardBeam beats every other class), test_no_side_bias_in_a_symmetric_battle (early/Laser mirror match goes 0-8 to side A over 8 seeds - the engine favours whichever side is placed first), test_a_bigger_budget_wins (a 1.5x budget wins only 3 of 8) and test_higher_tech_wins_at_equal_cost (Mega Disruptor tech 22 beats Mark IV Blaster tech 14 only 2 of 8). The side bias is the root of at least the last two: a fight the engine decides by placement cannot measure a budget or a tech edge. Out of scope for the boarding wave, which neither touched weapon damage nor changed the measured counts; fix: chase the placement/firing-order asymmetry in RonBattleEngine first, then re-measure the other three; tests/unit/test_battle_balance.py backend/server/battle/ron_battle_engine.py
  - 2026-07-28 reported: MEDIUM; tests/unit/test_battle_balance.py (untracked, added by an earlier agent in this wave) is 4 red at the boarding baseline and was red before any boarding change: test_no_weapon_class_dominates_at_equal_cost (early tier standardBeam beats every other class), test_no_side_bias_in_a_symmetric_battle (early/Laser mirror match goes 0-8 to side A over 8 seeds - the engine favours whichever side is placed first), test_a_bigger_budget_wins (a 1.5x budget wins only 3 of 8) and test_higher_tech_wins_at_equal_cost (Mega Disruptor tech 22 beats Mark IV Blaster tech 14 only 2 of 8). The side bias is the root of at least the last two: a fight the engine decides by placement cannot measure a budget or a tech edge. Out of scope for the boarding wave, which neither touched weapon damage nor changed the measured counts; fix: chase the placement/firing-order asymmetry in RonBattleEngine first, then re-measure the other three; tests/unit/test_battle_balance.py backend/server/battle/ron_battle_engine.py
  - 2026-07-28 3 of 4 fixed by DEF-30 (the side bias was the root, as this entry predicted): test_no_side_bias_in_a_symmetric_battle, test_a_bigger_budget_wins and 2 of the 3 higher-tech rows now pass. 2 remain and both are weapon-catalogue balance, not engine asymmetry - test_no_weapon_class_dominates_at_equal_cost (early standardBeam still unbeaten) and test_higher_tech_wins_at_equal_cost (Mega Disruptor 6 of 8, bar 7). Root cause of both carved out as DEF-32; not weakened
- [x] `DEF-30` **Battle movement overshoots its target, so the engagement drifts off the board and one side is systematically favoured** - RonBattleEngine._move_stacks moved a fixed-length vector (battle_speed * GRID_SCALE) with no cap at the distance to the target, so a closing stack flew PAST the thing it was closing on. Two stacks closing on each other then leapfrogged every round and the engaged pair travelled a full battle speed per round in the direction the first mover was heading - measured drifting to (2261, 2373) on a 1000-unit board - carrying the battle away from every stack that had not caught up. Because empire 1 moves first in the stack list, the drift always ran away from empire 1's corner and stranded its stragglers: mirror matches went 0-8 to side B on 9 of 11 equal-cost weapon rows (tests/unit/test_battle_balance.py::test_no_side_bias_in_a_symmetric_battle). Canon caps the step: PointUtilities.BattleMoveTo:224-247 steps an axis only while it is strictly farther away, so a stack converges on its target and stops.
  - 2026-07-28 reported: RonBattleEngine._move_stacks moved a fixed-length vector (battle_speed * GRID_SCALE) with no cap at the distance to the target, so a closing stack flew PAST the thing it was closing on. Two stacks closing on each other then leapfrogged every round and the engaged pair travelled a full battle speed per round in the direction the first mover was heading - measured drifting to (2261, 2373) on a 1000-unit board - carrying the battle away from every stack that had not caught up. Because empire 1 moves first in the stack list, the drift always ran away from empire 1's corner and stranded its stragglers: mirror matches went 0-8 to side B on 9 of 11 equal-cost weapon rows (tests/unit/test_battle_balance.py::test_no_side_bias_in_a_symmetric_battle). Canon caps the step: PointUtilities.BattleMoveTo:224-247 steps an axis only while it is strictly farther away, so a stack converges on its target and stops.
  - 2026-07-28 fixed: fixed - RonBattleEngine._cap_at_target shortens a closing step to the distance to the target, so a stack lands ON it and never past it, per PointUtilities.BattleMoveTo:224-247. A capped step lands the pursuer in the target's own square, which leaves a breaking-off stack with no 'away' to point at, so _break_off_heading picks a random bearing in that case - the canonical rule for a token that can neither open nor hold the range (Guts of the Battle Engine). Mirror matches now split 3-5, 4-4, 6-2 instead of 0-8; tests/unit/test_battle_engine.py::TestClosingMoveDoesNotOvershoot (4 tests, all confirmed failing first)
  - 2026-07-28 antisymmetry guard de-flaked: the 32-seed/0.5 pin sat 2.1 sigma from its own noise (8 clean 32-seed blocks ranged -0.25 to +0.63, one already past 0.5); recalibrated over 12 independent 128-seed blocks (no-bias mean +0.02, sd 0.20, range -0.18 to +0.35) and re-pinned at 128 seeds / tolerance 0.65 - 3.2 sigma above the no-bias mean, below the +0.84 low end of the proven bias signature; calibration recorded in the test comment, guard passes at +0.0525
  - 2026-07-28 the pre-existing TestClosingMoveDoesNotOvershoot flake diagnosed and de-flaked: two tests (closing-stop, run-down break-off) encoded a left-moves-first order that the equal-mass juggle makes a coin flip - a flake in the expectation, not the engine; fixed by the juggle's own rule (masses over 30 percent apart never swap): the stack the scenario needs first is now heavier (mass 200 vs 100), assertions unchanged; 10 of 10 repeat runs green
- [x] `DEF-31` **Brace posture pinned the fleet's unarmed hulls in the open** - The Brace gate in RonBattleEngine._move_stacks sat above the unarmed branch, so a braced fleet's freighters, tankers and colonisers held position instead of keeping out of weapon range. Brace fixes a FIRING position and an unarmed hull has none, so the order 'hold the line' also parked every support ship as a stationary target - a third of the anti-degeneracy task force. Measured effect: Brace was the worst posture in the round-robin at 47 wins against Standard 61, and scored 0 of 12 in the missile-boat mirror where the pinned freighters were simply farmed. Canon puts unarmed hulls on Default-Defense, whose 'initial behavior is to try to avoid combat entirely' (BATTLE.TXT).
  - 2026-07-28 reported: The Brace gate in RonBattleEngine._move_stacks sat above the unarmed branch, so a braced fleet's freighters, tankers and colonisers held position instead of keeping out of weapon range. Brace fixes a FIRING position and an unarmed hull has none, so the order 'hold the line' also parked every support ship as a stationary target - a third of the anti-degeneracy task force. Measured effect: Brace was the worst posture in the round-robin at 47 wins against Standard 61, and scored 0 of 12 in the missile-boat mirror where the pinned freighters were simply farmed. Canon puts unarmed hulls on Default-Defense, whose 'initial behavior is to try to avoid combat entirely' (BATTLE.TXT).
  - 2026-07-28 fixed: fixed - the Brace gate in _move_stacks now requires stack.is_armed, so an unarmed hull keeps the canonical run-from-the-guns behaviour. Un-pinning alone flipped Brace from worst (47) to unbeaten (71 against 44/43), so the posture also took the gunnery price its survivability was never paying: POSTURE_MODIFIERS['Brace'].damage_dealt 0.80. Final posture totals 54/54/55, spread 1.02, every posture beaten somewhere and a winner somewhere; tests/unit/test_battle_doctrine.py::TestPostures test_brace_does_not_pin_the_unarmed_hulls + test_brace_pays_for_its_survivability_in_gunnery (both confirmed failing first)
- [ ] `DEF-32` **Torpedo and missile classes never win at equal cost, under any battle plan** - tests/unit/test_battle_balance.py::test_no_weapon_class_dominates_at_equal_cost shows the missile group losing 0-16 to every other class at both tech tiers that carry one, and the torpedo group losing to beams and gatlings at all three. Not a doctrine artifact: measured with the stand-off tactic that is supposed to be the missile answer, mid-tier Juggernaut Missile still beat Mark IV Blaster 0 of 8 seeds, and 0 of 8 again with both sides standing off. Arithmetic behind it - at BUDGET 3000 the Juggernaut buys 15 hulls at power 150 x 20 percent accuracy = 450 effective throw weight, against the Blaster's 23 hulls at power 66 x 100 percent = 1518, and the missile fires at initiative 1 against 7 - so the range advantage (500 units against 200) buys only about 1.5 rounds of free fire before contact because both sides close at once. Consequence: the shipyard decision between weapon classes is fake, and no battle plan a commander writes rescues a torpedo fleet. Out of scope for the doctrine wave, which measured it but did not touch weapon damage or the catalogue.
  - 2026-07-28 reported: tests/unit/test_battle_balance.py::test_no_weapon_class_dominates_at_equal_cost shows the missile group losing 0-16 to every other class at both tech tiers that carry one, and the torpedo group losing to beams and gatlings at all three. Not a doctrine artifact: measured with the stand-off tactic that is supposed to be the missile answer, mid-tier Juggernaut Missile still beat Mark IV Blaster 0 of 8 seeds, and 0 of 8 again with both sides standing off. Arithmetic behind it - at BUDGET 3000 the Juggernaut buys 15 hulls at power 150 x 20 percent accuracy = 450 effective throw weight, against the Blaster's 23 hulls at power 66 x 100 percent = 1518, and the missile fires at initiative 1 against 7 - so the range advantage (500 units against 200) buys only about 1.5 rounds of free fire before contact because both sides close at once. Consequence: the shipyard decision between weapon classes is fake, and no battle plan a commander writes rescues a torpedo fleet. Out of scope for the doctrine wave, which measured it but did not touch weapon damage or the catalogue.
  - 2026-07-28 root cause found by damage audit: the missile /10 divisor (DEF-33) deletes 90 percent of torpedo hit power, whole-token destruction (DEF-35) prevents attrition from range; measured margins +0.98 -> +0.72 early / +0.94 -> +0.10 late with divisor lifted, +0.53 / +0.03 with per-ship kills added
  - 2026-07-28 DEF-33/34/35 landed in order, matrix re-measured after each. Final wins of 16 (both orders): early beam 16/8 vs torpedo/gatling, torpedo 0 everywhere; mid beam 16/16/16, torpedo beats missile 16, gatling beats torpedo 10; late is contested - torpedo 7 vs beam, 15 vs missile, gatling 13/12/15, no unbeaten class by margin (beam-vs-torpedo mean -0.01). Mean margins: early beam+0.76 over torpedo; mid beam +0.68/+0.80/+0.66 over all. Remaining reds are catalogue, not engine: Beta Torpedo (12 power, 45 pct, 34 cost) cannot pay for itself at any margin, mid-tier Juggernaut effective throw 450 vs Blaster 1518 as this entry computed, and test_higher_tech Mega Disruptor vs Mark IV Blaster reads 5 of 8 (bar 7) at mean margin +0.19 - the only marginal tech step, all other six pay 8 of 8 at +0.75 or better. Next lever is the weapon catalogue itself
  - 2026-07-28 final verification re-ran the class matrices fresh: byte-for-byte the close-out wins matrix (early beam+gatling unbeaten and torpedo 0 everywhere; mid beam 16/16/16 unbeaten and missile 0 everywhere; late fully contested 9/15/3 vs 7/15/4 vs 13/12/15); higher-tech bar re-confirmed red on the same single pairing (Mega Disruptor vs Mark IV Blaster 5 of 8) - the catalogue remains the binding constraint
  - 2026-07-29 engagement-range restoration landed (give-ground with board clamp, Net-Damage min-range fix, Salvo then Close, admiralty tactic defaults): the weapon-class matrix moved but the bar stays red - the dominant class now varies by run (observed 'early: torpedo beats every other class' and 'early: gatlingGun beats every other class' on consecutive full-suite runs, engine RNG unseeded), so stand-off converted the old torpedo-loses-everywhere shape into a contested-but-unstable early tier; still catalogue tuning
  - 2026-07-29 final verification re-ran the matrices fresh twice in separate processes (plus a PYTHONHASHSEED-forced third): byte-identical both times, so the 'dominant class varies by run, engine RNG unseeded' observation does not reproduce on the final tree - it was a mid-work artefact. Stable current matrix (wins of 16, both orders): early gatlingGun unbeaten (16/16), mid torpedo unbeaten (16/16/16, missile 0 vs torpedo), late torpedo unbeaten by wins (16 vs beam, 9 vs missile, 15 vs gatling) - the Balanced plan's Salvo then Close default converted torpedo from loses-everywhere into the dominant class at mid and late. Still catalogue tuning, stays open
- [x] `DEF-33` **missile hit power divided by 10 with no beam equivalent - 90 percent of torpedo damage deleted** - HIGH; _fire_missile divides hit_power by 10 (ron_battle_engine.py:1344, comment '10 rounds per turn') while _fire_beam applies no such scaling, so a beam delivers 376.5 armour points per shot against a missile's 10.0 on identical power x quantity x percent inputs - measured 30888 of 34320 offered torpedo points (90.0 percent) destroyed by this one line over 8 mid-tier seeds; no C# ancestor: BattleEngine.cs FireMissile (794-808) applies hit power directly, and the header-cited source ServerState/RonBattleEngine.cs does not exist in references/original-game/; internally inconsistent either way - the allocator sizes BOTH classes as power*quantity/10.0 (ron:1199,1203), so beams deliver 10x their own budget; root cause of DEF-32; fix: lift the missile /10 (or consciously rescale both classes - user decision); backend/server/battle/ron_battle_engine.py
  - 2026-07-28 reported: reported: found by damage-model audit wf_9bc92d11-252; full accounting in docs/research-damage-model.md
  - 2026-07-28 fixed: lifted the missile-only /10 in RonBattleEngine._fire_missile - hit power now applied directly as canon does (BattleEngine.cs:794-808); the allocator /10 at ron:1199/1203 left alone as allocation bookkeeping identical for both classes. Measured beam-vs-torpedo mean margin (8 seeds, both orders) +0.98 -> +0.73 early and +0.94 -> +0.04 late, matching the audit's +0.72/+0.10 prediction; mid beam-vs-missile +0.77. Seeded e2e rows that encoded the divisor rescaled with derivations (torpedo bleed-through 4 -> 1 destroyer, electronics ratio rows to single-tube attackers, missile-double ceiling x10)
- [x] `DEF-34` **battle damage never read back - fleets enter every battle at full armour** - HIGH; DEF-27 added _write_back_damage so surviving armour loss lands on ShipToken.damage_percent, but StackToken.from_ship_token (stack.py:53-78) rebuilds armor = token.armor * token.quantity without reading it, so 100 percent of surviving damage is erased at the next battle boundary - measured: a token leaving battle 1 at 910 of 2000 armour entered battle 2 at 2000; canon persists by reference (Stack.cs:125 'a reference to the actual token in the fleet'), making this a port regression; damage_percent's only consumers are the repair step and the UI; fix: derive the starting armour pool from damage_percent in from_ship_token; backend/server/battle/stack.py
  - 2026-07-28 reported: reported: found by damage-model audit; completes the round trip DEF-27 half-fixed
  - 2026-07-28 fixed: StackToken.from_ship_token now derives the entry armour pool from the fleet token's damage_percent (armor * quantity * (1 - dp/100)), restoring the persistence canon gets by reference (Stack.cs:125); round trip proven stable - tests/unit/test_battle_engine.py TestDamageSurvivesTheBattleBoundary shows battle 2 entering at battle 1's exit armour (350 of 600) and a fleet that fights nobody twice keeping damage_percent exactly, with _write_back_damage generalised to compound against the survivors' entry armour
- [x] `DEF-35` **no per-ship attrition - a token fights at full output until annihilated whole** - MEDIUM; quantity is never decremented by gunfire anywhere in the engine (only boarding capture, ron:582,615): a 7-ship token with 1750 pooled armour fires identical hit_power at 1749 damage and is destroyed whole at 1750, so free shots at range can never silence enemy guns and reach cannot convert into attrition - the mechanism behind stand-off being worthless; inherited from C#, which names the correct rule while declining it (BattleEngine.cs:857 'Should destroy whole ships first, then spread remaining damage', sibling FIXME at :713, both in the reference's BUGS.txt), while mines and storms already kill per-ship (turn_generator.py:1540,1348); fix: derive quantity = ceil(max(0,armor)/per_ship) after each armour hit and scale the shield pool with survivors - MUST land after the divisor fix or it measures as a no-op; backend/server/battle/ron_battle_engine.py backend/server/battle/stack.py
  - 2026-07-28 reported: reported: found by damage-model audit; canon-legal restoration per BUGS.txt; sequencing constraint measured (+0.98 unchanged if landed first)
  - 2026-07-28 fixed: per-ship attrition restored in RonBattleEngine._damage_armor: surviving = ceil(max(0,armor)/per_ship_entry) after every armour hit, quantity drops as ships die, the shield pool scales with the survivors, and kills land in battle.losses as they happen - the rule the C# names and declines (BattleEngine.cs:857 and :713, both in the reference's BUGS.txt) and the bookkeeping mines and storms already use (turn_generator.py:1540/:1348); _write_back_damage lands surviving quantity on the fleet token and the battle message now reads 'N x Design -> K destroyed, M survived at X% damage' from the new BattleReport.outcomes ledger; measured with divisor also lifted: late-tier beam-vs-torpedo margin +0.04 -> -0.01 (parity), early +0.76 (catalogue-bound, DEF-32); unit tests TestPerShipAttrition (3)
- [x] `DEF-36` **legacy saves bypass the admiralty doctrine - Default plan always closes to contact** - MEDIUM; seed_admiralty_plans skips empires whose plan list already has entries, and pre-doctrine saves carry default_battle_plan 'Default' with tactic Maximise Damage, so every fleet in a loaded game closes to contact regardless of the doctrine layer - the zero-decision path the doctrine wave built is broken for every existing game; found by the engagement-range adversarial panel; fix: migrate legacy empires to the six admiralty plans on load and remap default_battle_plan 'Default' to 'Balanced'; backend/services/game_manager.py backend/server/battle/battle_plan.py
  - 2026-07-28 reported: reported: found by wf_62e1c6e2-8db cognitive-load challenge; review section in docs/research-engagement-range.md
  - 2026-07-28 load path proven by verification: a regressed pre-doctrine save (bare Default plan without doctrine keys, no outcomes ledger, no boarding fields) loads cleanly, gains the six admiralty plans via seed_admiralty_plans, and generates a full turn; the remap of default_battle_plan Default to Balanced remains unimplemented, so loaded legacy fleets still close to contact - stays open on that half
  - 2026-07-29 fixed: fixed: _deserialize_state (game_manager.py) remaps a loaded empire default of 'Default' to DEFAULT_EMPIRE_PLAN ('Balanced') after seed_admiralty_plans fills the missing standard plans; seeding still never overwrites, so a commander's edit under a standard name survives and a save naming any other default keeps it; the legacy 'Default' plan itself stays in the list for fleets that reference it by name; unit tests TestLegacySaveDoctrineMigration (2, full create-serialize-mutate-reload round trip)
- [ ] `DEF-37` **doctrine and boarding constants tuned under the pre-DEF-33/35 damage model - two balance bars red after the fix wave** - MEDIUM; tests/unit/test_battle_degeneracy.py::test_no_posture_wins_every_matchup reads posture totals spread 1.29 against its 1.25 bar and tests/unit/test_boarding.py::test_a_boarding_heavy_force_does_not_beat_a_balanced_one records zero captures in any run; both PASS with the old damage model restored (A/B: /10 divisor back, read-back and attrition off) and fail with it fixed, so the constants in battle_plan.py (POSTURE_MODIFIERS, STANCE_MODIFIERS, BOARDING_*) were tuned against an engine that deleted 90 percent of torpedo damage and never killed ships out of a token - boarding in particular now loses its prizes to per-ship attrition before shields drop; fix: retune the doctrine and boarding constants under the fixed damage model - the balance sweep (scripts/balance_sweep.py, task 47) is the instrument; backend/server/battle/battle_plan.py
  - 2026-07-28 reported: found landing DEF-33/34/35; A/B isolation confirms the damage fixes moved both bars, not a regression in the doctrine machinery
  - 2026-07-28 re-confirmed by final verification: posture totals spread 1.29 vs 1.25 (Standard 58 / Brace 55 / Scatter 45), stance spread 1.167 within cap; doctrine liveness under the fixed engine is 9 of 9 force pairings live on BOTH axes vs the 3-of-9 pre-fix baseline, no unbeaten and no inert option - the retune target is the spread alone
  - 2026-07-29 engagement-range wave note: posture spread now 1.26 vs 1.25 bar (was 1.29), boarding still 0 captures; new admiralty tactic defaults put test_no_standard_plan_wins_every_matchup on the edge - Commerce Raid (now Salvo then Close) flagged 'never wins a matchup' on one full-suite run and passed the next, matching the review's D8 measurement (Commerce Raid 43 -> 37 under Salvo); fold the plan round-robin into the task-47 retune
  - 2026-07-29 final verification measured doctrine liveness on the engagement-range engine with the sweep's own analyse(): stance 7 of 9 force pairings live (dead: Missile Boat vs Sabre Wing both roles), posture 7 of 9 (dead: Beam Line vs Missile Boat both roles), vs the 3-of-9 pre-damage-fix baseline and the 9-of-9 reading of 2026-07-28 taken before give-ground landed; no unbeaten and no inert option on either axis, stance spread 1.154 inside the 1.25 cap, posture spread 1.255 re-confirmed red (degeneracy test prints 1.26) - the retune target for task 47 is the posture spread plus the two dead pairings
